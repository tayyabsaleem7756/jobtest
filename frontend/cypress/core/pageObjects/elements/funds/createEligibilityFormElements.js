const previewButton = '.tabs-cta-section button'
const addBlockButton = 'h4 button[type=button]'
const addCommentButton = '.textArea + div button'
const commentTextArea = '.textArea__control textarea'
const pageTitle = 'h2.page-title'
const flowDropdown = '.flow-select'
const reviewTextInput = 'input[name=needReviewText]'
const notEligibleTextInput = 'input[name=failureText]'
const alertDiv = 'div[role=alert]'
const closeModalButton = 'button[aria-label=Close]'

const createEligibilityFormElements = {
    previewButton,
    addBlockButton,
    addCommentButton,
    commentTextArea,
    pageTitle,
    flowDropdown,
    reviewTextInput,
    notEligibleTextInput,
    alertDiv,
    closeModalButton
}

module.exports = createEligibilityFormElements