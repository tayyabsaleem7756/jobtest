/* eslint-disable import/prefer-default-export */
import { styled } from '@mui/system'

const Cont = styled('div')({
	display: 'flex',
})

const Biography = styled('p')({
	width: '50%',
	margin: '0px',
	fontWeight: '500',
	fontSize: '14px',
	lineHeight: '20px',
})

export { Cont, Biography }
