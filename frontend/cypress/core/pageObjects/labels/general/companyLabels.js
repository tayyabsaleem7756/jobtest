const addDocumentButtonLabel = 'Add Document'
const requireOnceButtonLabel = 'Required Once'
const requiredSignButtonLabel = 'Required Sign'
const requiredWetSignButtonLabel = 'Required Wet Signature'
const requiredGPSignButtonLabel = 'Required GP Signature'

const companyLabels = {
    addDocumentButtonLabel,
    requireOnceButtonLabel,
    requiredSignButtonLabel,
    requiredWetSignButtonLabel,
    requiredGPSignButtonLabel
}

module.exports = companyLabels